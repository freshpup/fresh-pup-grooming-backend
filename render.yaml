# Render Configuration

# This file contains the configuration settings for Render deployment.

version: 1
services:
  - name: fresh-pup
    type: web
    env: node
    plan: starter
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
